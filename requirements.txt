fastapi==0.139.2
uvicorn==0.51.0
python-multipart==0.0.32
pydantic==2.13.4
pydantic-settings==2.14.2
sqlmodel==0.0.22
# Postgres driver — only needed when DATABASE_URL points at Postgres (prod).
# psycopg[binary]==3.2.3
# --- Optional AI providers (uncomment what you deploy with) ---
# anthropic==0.42.0         # Claude — translation + companion tutor
# openai==1.59.5            # Whisper STT / GPT fallback / TTS
# google-cloud-speech       # Yoruba STT
# google-cloud-texttospeech # Yoruba TTS (yo-NG)
